/**
 * This includes Gradle plugins, which establishes a platform
 * to use the Java Platform Module System (JPMS), >= 16,
 * and create LPhy extensions using Service Provider Interface (SPI).
 * It also defines LPhy extension conventions and share the build logic.
 *
 * @author Walter Xie
 */
package io.github.linguaphylo.platforms;