/**
 * This includes Gradle plugins, which establishes multiple platforms
 * to use the Java Platform Module System (JPMS), >= 16,
 * and which can help to create LPhy extensions working with
 * Service Provider Interface (SPI).
 * It also defines LPhy extension conventions and shares the build logic.
 *
 * @author Walter Xie
 */
package io.github.linguaphylo.platforms;